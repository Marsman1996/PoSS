// all strings are encoded in hex.

key = {unknown m-byte value}
add = {unknown m-byte value}
for (i = 0; i < len(txt); i += m)
  result[i:(i+m)] = txt[i:(i+m)] ^ key
  k = (result[i:(i+m)] + add) % (1<<8*m)
